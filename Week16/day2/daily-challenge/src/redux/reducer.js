const initState = {
    movies: [],
}

export const reducer = (state = initState, actions = {}) => {
    switch (actions.type) {
        // case value:
        default:
            return {...state};
    }
}